# nse-indicator-engine
NSE Indicator Engine - Auto fetch, match and report


---
## ⚠️ Token Expiry Reminder
**`GH_PAT` secret expires: 2026-06-23**
Before that date, generate a new PAT at https://github.com/settings/tokens and update:
1. `nse-indicator-engine` repo → Settings → Secrets → `GH_PAT`
2. `nse-btst-signals` repo → Settings → Secrets → `GH_PAT`
