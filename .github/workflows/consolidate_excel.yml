name: NSE Consolidate Excel

on:
  workflow_run:
    workflows: ["NSE Indicator Engine"]
    types: [completed]
  workflow_dispatch:
    inputs:
      force:
        description: "Reprocess all dates (overwrite existing Excel files)"
        required: false
        default: "false"

permissions:
  contents: write

jobs:
  consolidate:
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install pandas openpyxl pytz

      - name: Run consolidator
        run: |
          if [ "${{ github.event.inputs.force }}" = "true" ]; then
            python src/consolidate_to_excel.py --force
          else
            python src/consolidate_to_excel.py
          fi

      - name: Cleanup old Excel files (keep last 5)
        run: |
          cd consolidated_excel
          COUNT=$(ls -t *.xlsx 2>/dev/null | wc -l)
          if [ "$COUNT" -gt 5 ]; then
            ls -t *.xlsx | tail -n +6 | xargs rm -f
            echo "Removed $(($COUNT - 5)) old Excel files, kept 5"
          else
            echo "Only $COUNT Excel files, no cleanup needed"
          fi
          cd ..

      - name: Commit Excel files to repo
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase --autostash origin main
          git add consolidated_excel/
          git diff --cached --quiet || git commit -m "auto: consolidated Excel $(date +'%d-%b-%Y')"
          git push
